# Docs — coding reference (never ships)

Instruction manual for coder AI that never sees game output. Each file answers one question, no overlap.

**Read this order:**

1. FLOW.md — Goal + user behavior. Why API has 0 paths for gold/bank/bags/curr/vault/mail, who Voc (6 Voc- tanks 3H/3A second-monitor back-and-forth 4-10 exports/night Sat push), success, when data matters for Tonight Plan, omnipresent sink why. **And the return trip**: the loop is a round trip since 1.4.0 — the site hands an answer back as `wbc1!` and the Import tab acts on it under a click; the two clocks (the addon's ~30 events, automatic, versus the player's milestone), and why neither side may assume the other's crossing happened.
2. RESEARCH-REFERENCE.md — Midnight 12.1 best practices, Interface 120100, no Ace3/libstub, load order libs→core→data→ui, account-wide WarbandProDB guid-keyed, event-driven throttle .5s, C_Container new, C_Bank 5 tabs only at banker, CLEU dead + Secret Values taint, LibDeflate wb1! -_ std, Compartment Func, Settings modern, packager.
3. CONTRACT.md — wb1! law, versioning wb0/wb1/wb2, JSON top-level+CharacterObject shape, field rules seenAt unix sec null tolerant, banks null vs empty warbank seenByGuid, encoding json->deflate lvl9->b64url strip= -> wb1!+payload, validation DoS caps >20 chars per bundle >25KB reject, paging (droppedOverCap/page/pages, 1.8.0) for a warband larger than one bundle and why the importer must merge rather than replace, bump policy.
4. UI.md — One native window `WarbandProFrame` (1.5.0): ButtonFrameTemplate chrome + PanelTabButtonTemplate tabs Roster·Export·Import·Options. **Roster tab (1.9.0)** is the grid — characters as columns and tracked things as rows, SavedInstances' arrangement ported (self-first column sort, raids-above-dungeons rows, `killed/total` cells) while its LibQTip/Ace3 rendering is not; the model is `Roster.lua`, pure and tested, and an unread section draws an EMPTY cell rather than a `0`. Six columns then it pages. Rest: inherits player UI scale by construction, Esc closes combat queue fail closed. Export tab freshness dots ScrollFrame+EditBox MaxLetters(0) HighlightText After(0) Select All, page line "page 1 of 3 - /warband copy 2 for the next 20" when the warband exceeds one bundle (1.8.0); Import tab InputBoxTemplate paste + junk rows secure Disenchant; Options tab checkboxes over WarbandProDB.opts incl autoJunk merchant auto-open and the minimap button. Minimap button WarbandProMinimapButton hand-built no LibDBIcon, click export / right-click options / drag by angle, on by default. Web sink `[ ↻ sync / ↥ import ▼ ]` tries clipboard.readText() inside click auto-fill else textarea fallback preview 🟢🟡🔴 hover bag 2m bank 5d etc warn half stale Confirm upserts D1 toast.
5. TESTING.md — Pure vs impure split offline luacheck+busted+vector round-trip identical addon↔web then 5-min manual screenshot-parsable single char fresh multi 6 warbank shared combat safe vault match mem<2MB string 4-7KB edge list null guards.
6. QA.md — Checklist release 5-min BugSack empty taint 0 single count1 multi count6 warbank shared combat safe wb1! len 6420ish dots 6 green web preview same mem <2MB vault matches, result PASS/FAIL lines parseable.
7. CI.md — The two workflows as built: ci.yml on every push (luacheck, tools/validate.mjs, tools/vector.mjs, dry-run package uploaded as an artifact) and release.yml on a v* tag or a manual run (full CI, then BigWigsMods/packager → GitHub Release + CurseForge/Wago/WoWInterface). Also: where the tokens go, why project ids live in the .toc, and the semver rule tied to the wire version.
8. PROMPT.md — One copy-paste prompt /app+/addon paths read order locked 8 file list flat root fewest moving parts 10 Lua + vendor + pkgmeta + luacheckrc + workflows app side pure warband-import.ts + ImportModal.astro + Menubar sink + hotkey i keys.ts tests checklist manual PASS lines.
9. EXECUTION-READY.md — Final check this file summary, why optimized simple flow, locked vs open 5 before code not blocking, next 3 days pure->impure->UI acceptance.
10. APP-IMPORT.md — /app side proposal written against that repo's existing cache pattern: character_addon_cache + warband_bank_cache migration, POST /api/addon/import, pure src/lib/warband-import.ts decoder, why the decoded cap is 1MB and not 25KB.
11. POLICY.md — Distribution compliance. CurseForge defers to Blizzard's addon policy, so that is the binding document; each rule is mapped to either a tools/validate.mjs check that fails CI or a standing constraint. Read before adding anything to the in-game UI that looks like a link.
12. STORE.md — The project description to paste on CurseForge/Wago/WoWInterface, written against those moderation rules. Not the README — the README is for contributors.

Vectors: contract/vectors/v1-min.json minimal golden for CI identity, and v1-min.wb1 beside it — the encoded fixture the web decoder tests against, regenerated by `node tools/vector.mjs --write`. Add v1-full.json + v1-bundle-6.json from a real character later.

Where these docs and the code disagree, the code is right and the doc has been
corrected in place: see CONTRACT.md "Two corrections the code made" and "v1 as
implemented", and the size table under Security / DoS.

Nothing in here ships: .pkgmeta strips docs/ and tools/ out of the zip, so the trimming is automatic rather than a step someone has to remember. Root stays flat per your rule Problem→Install one paste→Use 2-3→What it catches→Inside 4-6 files.

EXECUTION-READY says ready.
